class Program
{
    public static void Main(string[] args)
    {

        //Student.ShowAll(Student.ShowAbove74());
        //Student.ShowAll(Student.ShowCourseStudent("Data Science"));
        //Student.ShowAll(Student.HighestToLow());
        //Student.ShowAll(new List<Student>{Student.Youngest()});

        //Console.WriteLine(Student.GetAvg());

        //Student.ShowAll(Student.SearchStudent(""));
        Student.ShowNames();
    }
}
